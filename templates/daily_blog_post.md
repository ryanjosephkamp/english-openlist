---
layout: post
title: "Daily English OpenList Update #{{ update_number }} ({{ date }})"
date: {{ full_timestamp }}
categories: [daily-updates]
tags: [daily, statistics, words, update-{{ update_number }}]
excerpt: "Daily update #{{ update_number }} — {{ promoted_count }} words promoted, new statistics, and full transparency report."
---

# Daily English OpenList Update #{{ update_number }} — {{ date }}

**Generated automatically at {{ timestamp }} UTC**

## Today's Results

- **Words promoted from invalid list**: {{ promoted_count }}
- **New words discovered today**: {{ new_words_total }}
- **Total valid words now**: **{{ total_valid_words:, }}**

{% if promoted_words %}
### Words Promoted Today

```text
{{ promoted_words }}
```
{% endif %}

## New English OpenList Statistics

### Current Totals
- **Total Valid Words**: **{{ total_valid_words:, }}**
- **Total Invalid Entries**: {{ total_invalid_entries:, }}

### Word Length Distribution

<div class="chart-container">
  <canvas id="lengthChart"></canvas>
</div>

### Starting Letter Distribution

<div class="chart-container">
  <canvas id="letterChart"></canvas>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
// Word Length Chart
const lengthCtx = document.getElementById('lengthChart');
new Chart(lengthCtx, {
  type: 'bar',
  data: {
    labels: {{ length_labels | safe }},
    datasets: [{
      label: 'Number of Words',
      data: {{ length_values | safe }},
      backgroundColor: 'rgba(54, 162, 235, 0.7)',
      borderColor: 'rgba(54, 162, 235, 1)',
      borderWidth: 1
    }]
  },
  options: {
    responsive: true,
    scales: { y: { beginAtZero: true } }
  }
});

// Starting Letter Chart
const letterCtx = document.getElementById('letterChart');
new Chart(letterCtx, {
  type: 'bar',
  data: {
    labels: {{ letter_labels | safe }},
    datasets: [{
      label: 'Number of Words',
      data: {{ letter_values | safe }},
      backgroundColor: 'rgba(255, 99, 132, 0.7)',
      borderColor: 'rgba(255, 99, 132, 1)',
      borderWidth: 1
    }]
  },
  options: {
    responsive: true,
    scales: { y: { beginAtZero: true } }
  }
});
</script>

<style>
.chart-container { max-width: 800px; margin: 20px auto; }
</style>

## Execution Transparency

**API Health Check**
- Merriam-Webster API calls: {{ api_calls_mw }} ({{ api_success_mw }}% success)
- Wordnik API calls: {{ api_calls_wordnik }} ({{ api_success_wordnik }}% success)

**Words Discovered by Source**
- Merriam-Webster RSS / New Words: {{ from_rss }}
- Wordnik Word of the Day: {{ from_wordnik }}
- Invalid list validation: {{ from_invalid }}
- Manual additions: {{ from_manual }}

*All systems operational. Pipeline completed successfully.*

## Update Links

- [View on Hugging Face](https://huggingface.co/datasets/ryanjosephkamp/english-openlist/tree/main/releases/{{ date }})
- [Download full release package](https://huggingface.co/datasets/ryanjosephkamp/english-openlist/tree/main/releases/{{ date }})

---

**Previous Update**: [Daily Update #{{ prev_number }}]({{ prev_url }})

*This post was automatically generated by the English OpenList daily pipeline.*